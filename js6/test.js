export function sum(a,b){
  return a+b;
}
export var pi=3.14;
var svg=SVG("main");
var rect = svg.rect(100, 100).attr({ fill: '#f06'})
class App {
  constructor(url,id){
    this.svg=SVG(id);
    this.status="initializing";
    this.rest={};
  }
}
